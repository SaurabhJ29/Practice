#include <stdio.h>
void printArray(int arr[], int n); //declartion of the function printArray.

int main()
{
    int arr[6]={1, 2, 3, -1, -2, -3};
    int n = sizeof(arr)/sizeof(arr[0]);
    printf("\nThe size of array is: %d", n);
    //printf("\nThe element of the arr are:");
    //printArray(arr, n);
    
    int i=0, j=0, k=0, posArr[n/2], negArr[n/2];
    
    //Calculation for posArr:
    for(i=0; i<n; i++){
        if(arr[i]>0){
            posArr[j]=arr[i];
            j++;
        }
    }
    //printf("\nThe element of the posArr are:");
    //printArray(posArr, n/2);
    
    //Calculation for negArr
    for(i=0; i<n; i++){
        if(arr[i]<0){
            negArr[k]=arr[i];
            k++;
        }
    }
    //printf("\nThe element of the negArr are:");
    //printArray(negArr, n/2);
    
    int newArr[n];
    int p=0;
    for(i=0; i<n; i++){
        if(i%2==0){
        newArr[i]=posArr[i/2];
        printf("\nThe %d element of newArr is: %d", i, newArr[i]);}
        else{
        newArr[i]=negArr[i/2];
        printf("\ni/2 is %d", i/2);
        printf("\nThe %d element of newArr is: %d", i, newArr[i]);}
    }
    printf("\nThe element of the New Array are:");
    printArray(newArr, n);

    return 0;
}

void printArray(int arr[6], int n){ //defintion of the function printArray.

    for(int i=0; i<n; i++){
        printf("\nThe %d element of array is: %d", i, arr[i]);
    }
}



//Output:

The size of array is: 6
The 0 element of newArr is: 1
i/2 is 0
The 1 element of newArr is: -1
The 2 element of newArr is: 2
i/2 is 1
The 3 element of newArr is: -2
The 4 element of newArr is: 3
i/2 is 2
The 5 element of newArr is: -3
The element of the New Array are:
The 0 element of array is: 1
The 1 element of array is: -1
The 2 element of array is: 2
The 3 element of array is: -2
The 4 element of array is: 3
The 5 element of array is: -3